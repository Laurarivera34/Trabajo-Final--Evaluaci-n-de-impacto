---
title: "Trabajo final"
author: "Maria Camila Caraballo, Laura Sarif Rivera, Zaira Garcia"
output:
  pdf_document:
    latex_engine: xelatex
  word_document: default
geometry: top=1.5cm, bottom=1.5cm, left=2cm, right=2cm
output_dir: outputs
header-includes: \usepackage{adjustbox}
---

```{r setup, include= FALSE, echo=FALSE, message=FALSE, warning=FALSE}
knitr::opts_chunk$set(
  echo = FALSE,
  message = FALSE,
  warning = FALSE
)
knitr::opts_knit$set(root.dir = "C:/1. MECA/2025/EVALUACIACÓN DE IMPACTO- Raachid Raaj/Trabajo final/Trabajo-Final--Evaluaci-n-de-impacto")

tinytex::tlmgr_update(self = TRUE, all = TRUE)

if (!requireNamespace("tinytex", quietly = TRUE)) {
  install.packages("tinytex")
}
# Verificar si TinyTeX está instalado
if (tinytex::is_tinytex()) {
  message("TinyTeX detectado. Actualizando instalación...")

  # Actualizar gestor de paquetes (self) y todos los paquetes
  tinytex::tlmgr_update(self = TRUE, all = TRUE)

  message("✅ TinyTeX actualizado correctamente.")
} else {
  message("No se detecta TinyTeX. Si usas MikTeX o TeX Live, actualiza manualmente.")
}
```

```{r results='asis', echo=FALSE}
# Cargar liberias y paquetes

# Instalando pacman si se requiere
if (!require("pacman")) install.packages("pacman")

# Cargando los paquetes usando pacman
pacman::p_load(
  
  # A) Manipulación, limpieza y visualización de datos
  tidyverse,    # Colección de paquetes esenciales (incluye dplyr, ggplot2, etc.)
  dplyr,        # Manipulación de datos (ya incluido en tidyverse, pero lo dejamos explícito).
  ggplot2,      # Visualización de datos (incluido en tidyverse).
  scales,       # Manejo de escalas (ej. porcentajes, comas).
  skimr,        # Resúmenes estadísticos rápidos de data.frames.
  
  # B) Modelado y análisis estadístico/econométrico
  fixest,       # Modelos de efectos fijos de alto rendimiento.
  plm,          # Modelos para datos de panel.
  AER,          # Herramientas de econometría aplicada.
  lmtest,       # Pruebas de hipótesis para modelos de regresión.
  sandwich,     # Errores estándar robustos.
  survey,       # Análisis de encuestas complejas.
  MatchIt,      # Propensity Score Matching
  cobalt,
  lfe,
  
  # C) Importación, exportación y generación de reportes
  haven,        # Lectura/escritura de Stata, SAS, SPSS.
  stargazer,    # Tablas de regresión con formato publicable.
  knitr,        # Reportes dinámicos en RMarkdown.
  modelsummary, # Tablas y resúmenes comparativos de modelos.
  tinytex,      # Soporte para compilar a PDF con LaTeX en RMarkdown.
  readxl,
  kableExtra,
  janitor       #clean names
)

# Leer archivo excel
educacion <- read_excel("Datos/PANEL_DE_EDUCACION(2023).xlsx")
salud_servicios <- read_excel("Datos/PANEL_SALUD_Y_SERVICIOS.xlsx")
violencia <- read_excel("Datos/PANEL_CONFLICTO_Y_VIOLENCIA(2024).xlsx")
gobierno <- read_excel("Datos/PANEL_BUEN_GOBIERNO(2024).xlsx")
caracteristicas <- read_excel("Datos/PANEL_CARACTERISTICAS_GENERALES(2024).xlsx")
mpiopdet <- read_excel("Datos/MunicipiosPDET.xlsx")
turismo <- read_excel("Datos/turismo.xlsx")

```

## Paso 1. Limpieza de datos

```{r}
# estandarizar nombres de variables
salud_servicios <- salud_servicios %>% clean_names()
turismo <- turismo %>% clean_names()

# renombrar variables 
salud_servicios <- salud_servicios %>%
  rename(codmpio = c_digo_dane_del_municipio,
         ano = a_o)

# convertirlo a numérico
educacion <- educacion %>%
  mutate(across(c(ano, codmpio), as.numeric))

salud_servicios <- salud_servicios %>%
  mutate(across(c(ano, codmpio), as.numeric))

violencia <- violencia %>%
  mutate(across(c(ano, codmpio), as.numeric))

gobierno <- gobierno %>%
  mutate(across(c(ano, codmpio), as.numeric))

caracteristicas <- caracteristicas %>%
  mutate(across(c(ano, codmpio), as.numeric))

# convertir a numérico
turismo <- turismo %>%
  mutate(across(c(ano, codmpio), as.numeric))

# pegar bases
base_total <- educacion %>%
  full_join(salud_servicios, by = c("codmpio", "ano")) %>%
  full_join(violencia, by = c("codmpio", "ano")) %>%
  full_join(gobierno, by = c("codmpio", "ano")) %>%
  full_join(caracteristicas, by = c("codmpio", "ano"))


# estandarizar nombres de variables
base_total <- base_total %>% clean_names()

# base CEDE
datoscede <- base_total %>%
  select(
    codmpio, ano,
    mdm_g_ejecur,
    df_desemp_fisc,
    mdm_g_gobayt,
    gandina, gcaribe, gpacifica, gorinoquia, gamazonia,
    sisben_pobl_1, sisben_pobl_2, sisben_pobl_3, sisben_pobl_4,
    sisben_pobl_5, sisben_pobl_6, sisben_pobl_7, sisben_pobl_8,
    sisben_pobl_9, sisben_pobl_10,
    altura, discapital, indrural,
    homicidios, extorsion, errad_manual, o_desap_for, o_secuest, t_establ_o
  )

# años seleccionados para el análisis
datoscede <- datoscede %>%
  filter(ano >= 2013 & ano <= 2021)

# para base larga con la variable de regiones y sisben
#datoscede <- datoscede%>%
#  pivot_longer(
 #   cols = c(gandina, gcaribe, gorinoquia, gpacifica, gamazonia),     names_to = "region",     values_to = "dummy_region"      
  #)

datoscede <- datoscede %>%
  mutate(
    region = case_when(
      gandina == 1 ~ "Andina",
      gcaribe == 1 ~ "Caribe",
      gpacifica == 1 ~ "Pacífica",
      gorinoquia == 1 ~ "Orinoquía",
      gamazonia == 1 ~ "Amazonía",
      TRUE ~ NA_character_
    )
  )

datoscede <- datoscede %>%
  select(-gandina, -gcaribe, -gpacifica, -gorinoquia, -gamazonia)

# unir con mpio pdet
mpiopdet <- mpiopdet %>%
  rename(
    codmpio = `Código DANE Municipio`
  ) %>%
  mutate(
    codmpio = as.numeric(codmpio),
    pdet = 1
  ) %>%
  select(codmpio,pdet)

# se une la base de mpio pdet para clasificarlos 
datoscede <- datoscede %>%
  left_join(mpiopdet, by = "codmpio") %>%
  mutate(pdet = if_else(is.na(pdet), 0, 1))

# contar filas por municipio y año
duplicados <- datoscede %>%
  count(codmpio, ano) %>%
  filter(n > 1)

# se revisa los mpios pdet por cada año
conteo_pdet <- datoscede%>%
  group_by(ano) %>%
  summarise(
    municipios_pdet = sum(pdet == 1, na.rm = TRUE),
    .groups = "drop"
  )

#____________

# Porcentaje de NAs por variable (columna)
colMeans(is.na(datoscede)) * 100

# se eliminan las variables con mayor prop de na
vars_a_eliminar <- c("extorsion", "errad_manual", "o_desap_for", "o_secuest")

datoscede <- datoscede %>%
  select(-any_of(vars_a_eliminar))

#Tabla auxiliar para graficar sisben 
datos_sisben <- datoscede

#Formato largo para el gráfico
datos_sisben <- datos_sisben %>%
  # 1. FILTRAR por el año correcto
  filter(ano == 2016) %>%
  
  # 2. TRANSFORMAR de ANCHA a LARGA (Pivot)
  pivot_longer(
    cols = starts_with("sisben_pobl_"),
    names_to = "Grupo_Edad_Num",        
    values_to = "Poblacion"             
  ) %>%
  
  # 3. LIMPIAR y ETIQUETAR (Crear categorías legibles)
  mutate(
    # Aseguramos que la clasificación PDET sea un factor y se llame 'Clasificacion_PDET'
    Clasificacion_PDET = factor(pdet),
    
    # Mapeo de los 10 grupos de edad (AJUSTA LOS RANGOS DE EDAD SI SON DIFERENTES)
    Grupo_Edad = case_when(
      Grupo_Edad_Num == "sisben_pobl_1" ~ "0-9 años",
      Grupo_Edad_Num == "sisben_pobl_2" ~ "10-19 años",
      Grupo_Edad_Num == "sisben_pobl_3" ~ "20-29 años",
      Grupo_Edad_Num == "sisben_pobl_4" ~ "30-39 años",
      Grupo_Edad_Num == "sisben_pobl_5" ~ "40-49 años",
      Grupo_Edad_Num == "sisben_pobl_6" ~ "50-59 años",
      Grupo_Edad_Num == "sisben_pobl_7" ~ "60-69 años",
      Grupo_Edad_Num == "sisben_pobl_8" ~ "70-79 años",
      Grupo_Edad_Num == "sisben_pobl_9" ~ "80-89 años",
      Grupo_Edad_Num == "sisben_pobl_10" ~ "90+ años",
      TRUE ~ "No Clasificado"
    )
  ) %>%
  # Asegurar el orden correcto de los factores para el gráfico
  mutate(
      Grupo_Edad = factor(Grupo_Edad, levels = unique(Grupo_Edad))
  )

#Estandarizacion de datos para comparación
datos_sisben <- datos_sisben %>%
  # A. Agregamos el conteo de Sisbén a nivel de Grupo_Edad y Clasificacion_PDET
  group_by(Clasificacion_PDET, Grupo_Edad) %>%
  summarise(
    Total_Sisben = sum(Poblacion, na.rm = TRUE),
    .groups = 'drop'
  ) %>%
  
  # B. Normalizamos dentro de cada Clasificacion_PDET:
  # Calculamos el % que representa cada Grupo_Edad sobre el Total_Sisben en su clasificación.
  group_by(Clasificacion_PDET) %>%
  mutate(
      Proporcion_Total_Sisben = Total_Sisben / sum(Total_Sisben)
  ) %>%
  ungroup() %>%
  mutate(
      # Asegurar que el eje Y esté en el orden de los grupos de edad
      Grupo_Edad = factor(Grupo_Edad, levels = unique(Grupo_Edad)) 
  )
```

```{r}
#Grafico sisben
g_sisben_proporcion <- datos_sisben %>%
  ggplot(aes(x = Grupo_Edad, y = Proporcion_Total_Sisben, fill = Clasificacion_PDET)) +
  
  # Barras de conteo, posicionadas lado a lado
  geom_bar(stat = "identity", position = position_dodge(width = 0.8)) +
  
  # Transformar a horizontal
  coord_flip() +
  
  labs(
    title = "Estructura de Edad de la Población Sisbén (Año 2016)",
    subtitle = "Proporción de cada grupo sobre el Sisbén Total",
    y = "Proporción de Población Sisbén Total",
    x = "Grupo de Edad",
    fill = "Municipio PDET"
  ) +
  
  # Formatear el eje Y como porcentaje
  scale_y_continuous(labels = percent) + 
  
  theme_minimal() +
  theme(
    plot.title = element_text(face = "bold", size = 14),
    legend.position = "bottom"
  )

print(g_sisben_proporcion)
ggsave(
  filename = "Graficos-tablas/g_sisben_proporcion_final.png", 
  plot = g_sisben_proporcion,                       
  width = 10,                                       
  height = 7,                                        
  dpi = 300                                          
)
```

```{r}
# Creamos una base auxiliar para visualizar la diferencia 
datos_discapital <- datoscede

datos_discapital <- datos_discapital%>%
  # Mantener solo una observación por municipio si la base es tipo panel
  distinct(codmpio, .keep_all = TRUE) %>% 
  
  # Seleccionar solo las variables relevantes y limpiar NAs
  select(discapital, pdet) %>%
  filter(!is.na(discapital)) %>%
  
  # Asegurar que la clasificación PDET sea factor para el eje X
  mutate(pdet_clasificacion = factor(pdet, labels = c("No PDET", "PDET")))

# 2. Generación del Box Plot
g_discapital_pdet <- datos_discapital %>%
  ggplot(aes(x = pdet_clasificacion, y = discapital, fill = pdet_clasificacion)) +
  
  # Usar geom_boxplot
  geom_boxplot(width = 0.5, alpha = 0.7) +
  
  # Añadir puntos de datos (jitter) para ver la densidad subyacente (opcional)
  geom_jitter(width = 0.1, size = 0.5, alpha = 0.2, color = "gray20") +
  
  labs(
    title = "Distancia lineal a la capital del departamento: Municipios PDET vs. No PDET",
    subtitle = "Comparación de la distribución de la distancia",
    x = NULL, # Ya se explica en el título/leyenda
    y = "Distancia a la Capital (km)",
    fill = "Clasificación"
  ) +
  theme_minimal() +
  theme(
    plot.title = element_text(face = "bold"),
    legend.position = "none" # La leyenda es redundante si se usa el eje X para la etiqueta
  )

print(g_discapital_pdet)
ggsave(
  filename = "Graficos-tablas/g_discapital_pdet.png", 
  plot = g_discapital_pdet,                       
  width = 10,                                       
  height = 7,                                        
  dpi = 300                                          
)
```

\

## Paso 1.1 Estadísticas Descriptivas inicial

```{r}
# estadísticas descriptivas por grupo PDET
vars_interes <- c("indrural", "discapital", "df_desemp_fisc",
                  "homicidios", "t_establ_o")

nombres_descriptivos <- c(
  "Porcentaje rural",
  "Porcentaje en capital",
  "Desempeño fiscal (%)",
  "Número de homicidios",
  "Establecimientos oficiales"
)

# Crear tabla vertical con estadísticas
tabla_aer <- datoscede %>%
  filter(pdet %in% c(0,1)) %>%
  select(pdet, all_of(vars_interes)) %>%
  pivot_longer(
    cols = all_of(vars_interes),
    names_to = "variable",
    values_to = "valor"
  ) %>%
  group_by(pdet, variable) %>%
  summarise(
    Min = min(valor, na.rm = TRUE),
    Max = max(valor, na.rm = TRUE),
    Promedio = mean(valor, na.rm = TRUE),
    .groups = "drop"
  ) %>%
  pivot_wider(
    names_from = pdet,
    values_from = c(Min, Max, Promedio),
    names_prefix = "pdet_"
  ) %>%
  # Reemplazar nombres de variables por descriptivos
  mutate(variable = nombres_descriptivos)

# Crear tabla AER
tabla_html <- tabla_aer %>%
  kable(
    caption = "Estadísticas descriptivas por municipio PDET y No PDET",
    digits = 2,
    format = "html",
    col.names = c(
      "Variable",
      "Min (No PDET)", "Max (No PDET)", "Promedio (No PDET)",
      "Min (PDET)", "Max (PDET)", "Promedio (PDET)"
    )
  ) %>%
  kable_classic(full_width = FALSE, html_font = "Cambria")

# guardar 
save_kable(tabla_html, "Graficos-tablas/tabla_estadisticas_pdet.html")

# análisis univariado
grafico_pdet <- datoscede %>%
  filter(pdet %in% c(0,1)) %>%
  pivot_longer(cols = all_of(vars_interes), names_to = "variable", values_to = "valor") %>%
  mutate(variable = factor(variable, 
                           levels = vars_interes, 
                           labels = nombres_descriptivos)) %>%
  ggplot(aes(x = factor(pdet), y = valor, fill = factor(pdet))) +
  geom_boxplot() +
  facet_wrap(~variable, scales = "free") +
  scale_fill_manual(values = c("0" = "lightblue", "1" = "salmon"), labels = c("No PDET", "PDET")) +
  labs(x = "Grupo PDET", y = "Valor", fill = "Grupo") +
  theme_minimal()

# Guardar el gráfico
ggsave("Graficos-tablas/grafico_boxplot_pdet.png", grafico_pdet, width = 10, height = 7, dpi = 300)

# Tabla de estadísticas descriptivas de homicidios por grupo PDET
tabla_homicidios <- datoscede %>%
  filter(!is.na(pdet), !is.na(homicidios)) %>%
  group_by(codmpio, pdet) %>%                     # 🔹 agrupa por municipio y grupo PDET
  summarise(homicidios_total = sum(homicidios, na.rm = TRUE), .groups = "drop") %>%
  group_by(pdet) %>%
  summarise(
    N_municipios = n(),                            # ahora sí cuenta municipios únicos
    Min = min(homicidios_total),
    Q1 = quantile(homicidios_total, 0.25),
    Promedio = mean(homicidios_total),
    Mediana = median(homicidios_total),
    Q3 = quantile(homicidios_total, 0.75),
    Max = max(homicidios_total),
    Desviación = sd(homicidios_total)
  ) %>%
  mutate(Grupo = ifelse(pdet == 1, "PDET", "No PDET")) %>%
  select(Grupo, everything(), -pdet)

# Tabla HTML
tabla_html <- tabla_homicidios %>%
  kable(
    caption = "Estadísticas descriptivas de homicidios por grupo PDET (nivel municipal)",
    digits = 2,
    col.names = c("Grupo", "N", "Mínimo", "1er Cuartil", "Promedio", "Mediana", "3er Cuartil", "Máximo", "Desviación estándar")
  ) %>%
  kable_classic(full_width = FALSE, html_font = "Cambria")

# Guardar como HTML
save_kable(tabla_html, "Graficos-tablas/tabla_homicidios.html")
```

```{r}
# Análisis univariado del índice de ruralidad por grupo pdet
# Limpieza de datos 
datoscede <- datoscede %>%
  mutate(codmpio = as.integer(codmpio)) %>%       
  distinct(codmpio, pdet, indrural, .keep_all = TRUE)

# Consolidar base a nivel municipal
datoscede_mpio <- datoscede %>%
  filter(!is.na(codmpio)) %>%
  group_by(codmpio) %>%
  summarise(
    pdet = max(pdet, na.rm = TRUE),
    indrural = mean(indrural, na.rm = TRUE),
    .groups = "drop"
  ) %>%
  mutate(pdet = ifelse(is.infinite(pdet), 0, pdet))

# Revisar número de municipios PDET
conteo <- datoscede_mpio %>% count(pdet)
print(conteo)

# Tabla descriptiva
tabla_indrural <- datoscede_mpio %>%
  group_by(pdet) %>%
  summarise(
    Municipios = n(),
    Con_dato = sum(!is.na(indrural)),
    Promedio = mean(indrural, na.rm = TRUE),
    Mediana = median(indrural, na.rm = TRUE),
    Desviación_estándar = sd(indrural, na.rm = TRUE),
    Mínimo = min(indrural, na.rm = TRUE),
    `1er_Cuartil` = quantile(indrural, 0.25, na.rm = TRUE),
    `3er_Cuartil` = quantile(indrural, 0.75, na.rm = TRUE),
    Máximo = max(indrural, na.rm = TRUE),
    .groups = "drop"
  ) %>%
  mutate(Grupo = ifelse(pdet == 1, "PDET", "No PDET")) %>%
  select(Grupo, everything(), -pdet)

# Exportar tabla descriptiva a HTML 
tabla_html <- tabla_indrural %>%
  kable(
    caption = "Estadísticas descriptivas del índice de ruralidad por grupo PDET",
    digits = 3,
    col.names = c("Grupo", "Municipios", "Con dato", "Promedio", "Mediana",
                  "Desviación estándar", "Mínimo", "1er Cuartil", "3er Cuartil", "Máximo")
  ) %>%
  kable_classic(full_width = FALSE, html_font = "Cambria")

# Guardar tabla HTML
save_kable(tabla_html, file = "Graficos-tablas/tabla_indrural.html")

# Gráfico comparativo 
grafico_indrural <- datoscede_mpio %>%
  mutate(Grupo = ifelse(pdet == 1, "PDET", "No PDET")) %>%
  ggplot(aes(x = Grupo, y = indrural, fill = Grupo)) +
  geom_boxplot(alpha = 0.7, width = 0.6) +
  scale_fill_manual(values = c("skyblue", "seagreen3")) +
  labs(
    title = "Distribución del índice de ruralidad por grupo PDET",
    x = "",
    y = "Índice de ruralidad"
  ) +
  theme_minimal(base_family = "Cambria") +
  theme(legend.position = "none")

# Guardar gráfico
ggsave("Graficos-tablas/grafico_indrural.png", grafico_indrural, width = 7, height = 5, dpi = 300)

```

## Paso 3 Construcción base completa con turismo

```{r}
# Anexo de variables con información de poblacion y valor agregado

#Copia  para hacer las modificaciones 
datos_borrador <- datoscede

#Cargamos la base que contiene la poblacion
datos_poblacion <- read_excel("Datos/Total_pob_mun.xlsx")

#Estandarizamos las variables
datos_poblacion<- datos_poblacion %>%
  rename(
    codmpio = MPIO,   
    ano      = AÑO,       
    poblacion = Población    
  )

#Eliminamos las variables sin uso
datos_poblacion <- datos_poblacion %>%
  select(-`DP`, -`DPNOM`, -`DPMP`, -`ÁREA GEOGRÁFICA`)

#Diccionario de regiones
region_dic <- datoscede %>%
  select(codmpio, region) %>%
  distinct()

#Modificacion del diccionario
region_dic <- region_dic %>%
  mutate(codmpio = as.character(codmpio))

#Estandarizar formato
datos_poblacion <- datos_poblacion %>%
  mutate(
    codmpio = str_remove(as.character(codmpio), "^0")
  )

#Juntamos la base con el diccionario
datos_poblacion <- datos_poblacion %>%
  left_join(region_dic, by = "codmpio")

#Verificacion del pegue 
faltantes_region <- datos_poblacion %>% filter(is.na(region)) %>% distinct(codmpio)

#Estandarizar formato
datos_borrador <- datos_borrador %>%
  mutate(
    codmpio = as.character(codmpio),
    ano = as.integer(ano)
  )

datos_poblacion <- datos_poblacion %>%
  mutate(
    codmpio = as.character(codmpio),
    ano = as.integer(ano)
  )

#Unimos las bases
base_final <- datos_borrador %>%
  left_join(
    datos_poblacion %>% select(codmpio, ano, poblacion), 
    by = c("codmpio","ano")
  )

base_final %>% filter(is.na(poblacion)) %>% count()

#Verificacion
base_final %>% filter(is.na(poblacion)) %>% count()

```

```{r}
#Cargar base de valor agregado para los municipios 
total_VA<- read_excel("Datos/valor_agregado_mun.xlsx", sheet = "Total") %>%
  pivot_longer(
    cols = matches("20\\d{2}"),
    names_to = "ano",
    values_to = "valor_agregado_total"
  )

#Estandarizar el formato
total_VA<- total_VA %>%
  rename(
    codmpio = `Código Municipio`,   
  )

total_VA <- total_VA %>%
  select(-`Municipio`, -`Código Departamento`, -`Departamento`,)

total_VA <- total_VA %>%
  mutate(
    codmpio = str_remove(as.character(codmpio), "^0")
  )

#Cargar el resto de las hojas
years <- as.character(2013:2022)

va_sector <- map_dfr(
  years,
  ~ read_excel("Datos/valor_agregado_mun.xlsx", sheet = .x) %>%
      mutate(ano = as.integer(.x))
)

#Estandarizar el formato
va_sector<- va_sector %>%
  rename(
    codmpio = `Código Municipio`,
    act_primarias = `Actividades primarias *`,
    act_secundarias = `Actividades secundarias **`,
    act_terciarias = `Actividades terciarias ***`,
    peso_relativo = `Peso relativo municipal en el valor agregado departamental (%)`
  )

va_sector <- va_sector %>%
  select(-`Municipio`, -`Código Departamento`, -`Año`,-`Departamento`,-`Valor agregado\r\n`)


va_sector <- va_sector %>%
  mutate(
    codmpio = str_remove(as.character(codmpio), "^0")
  )


#Estandarizar formatos
base_final <- base_final %>%
  mutate(
    codmpio = as.character(codmpio),
    ano = as.integer(ano)
  )


va_sector <- va_sector %>%     
  mutate(
    codmpio = as.character(codmpio),
    ano = as.integer(ano)
  )


turismo <- turismo %>%     
  mutate(
    codmpio = as.character(codmpio),
    ano = as.integer(ano)
  )

#Unir bases 
base_completa <- base_final %>%
  left_join(
    va_sector,
    by = c("codmpio", "ano")
  )

base_completa <- base_completa %>%     
  mutate(
    codmpio = as.character(codmpio),
    ano = as.integer(ano)
  )

# Se pega la base completa con la de turismo
base_completa <- base_completa %>%
  full_join(turismo, by = c("codmpio", "ano"))

# Se deja NA los valores faltantes
base_completa <- base_completa %>%
  mutate(across(
    where(is.character),
    ~ na_if(trimws(.), "NA")
  ))
# Nuevas variables a numerica 
base_completa <- base_completa %>%
  mutate(across(
    c(llegadas_pasajeros_nacionales,
      llegadas_internacionales,
      visitantes_extranjeros_no_residentes,
      visitantes_pnn,
      prestadores_de_servicios_turisticos_activos,
      establecimientos_de_alojamiento_y_hospedaje),
    as.numeric
  ))
```

## Paso 3.1 Estadísticas Descriptivas con la base completa

```{r}
# estadísticas descriptivas 
vars_interes_compl <- c("indrural", "discapital", "df_desemp_fisc",
                  "homicidios", "t_establ_o", "act_primarias", 
                  "act_secundarias", "act_terciarias",
                  "visitantes_extranjeros_no_residentes")

nombres_descriptivos_compl <- c(
  "Porcentaje rural",
  "Porcentaje en capital",
  "Desempeño fiscal (%)",
  "Número de homicidios",
  "Establecimientos oficiales",
  "Actividades primarias",
  "Actividades secundarias",
  "Actividades terciarias",
  "Visitantes extranjeros no residentes"
)

# Crear tabla vertical con estadísticas
tabla_aer_compl <- base_completa %>%
  filter(pdet %in% c(0,1)) %>%
  select(pdet, all_of(vars_interes_compl)) %>%
  pivot_longer(
    cols = all_of(vars_interes_compl),
    names_to = "variable",
    values_to = "valor"
  ) %>%
  group_by(pdet, variable) %>%
  summarise(
    Min = min(valor, na.rm = TRUE),
    Max = max(valor, na.rm = TRUE),
    Promedio = mean(valor, na.rm = TRUE),
    .groups = "drop"
  ) %>%
  pivot_wider(
    names_from = pdet,
    values_from = c(Min, Max, Promedio),
    names_prefix = "pdet_"
  ) %>%
  # Reemplazar nombres de variables por descriptivos
  mutate(variable = nombres_descriptivos_compl)

# Crear tabla AER
tabla_html_compl <- tabla_aer_compl %>%
  kable(
    caption = "Estadísticas descriptivas por municipio PDET y No PDET",
    digits = 2,
    format = "html",
    col.names = c(
      "Variable",
      "Min (No PDET)", "Max (No PDET)", "Promedio (No PDET)",
      "Min (PDET)", "Max (PDET)", "Promedio (PDET)"
    )
  ) %>%
  kable_classic(full_width = FALSE, html_font = "Cambria")

# guardar 
save_kable(tabla_html_compl, "Graficos-tablas/tabla_estadisticas_complet_pdet.html")

# análisis univariado
grafico_pdet_compl <- base_completa %>%
  filter(pdet %in% c(0,1)) %>%
  pivot_longer(cols = all_of(vars_interes_compl), names_to = "variable", values_to = "valor") %>%
  mutate(variable = factor(variable, 
                           levels = vars_interes_compl, 
                           labels = nombres_descriptivos_compl)) %>%
  ggplot(aes(x = factor(pdet), y = valor, fill = factor(pdet))) +
  geom_boxplot() +
  facet_wrap(~variable, scales = "free") +
  scale_fill_manual(values = c("0" = "lightblue", "1" = "salmon"), labels = c("No PDET", "PDET")) +
  labs(x = "Grupo PDET", y = "Valor", fill = "Grupo") +
  theme_minimal()

# Guardar el gráfico
ggsave("Graficos-tablas/grafico_compl_boxplot_pdet.png", grafico_pdet_compl, width = 10, height = 7, dpi = 300)

# Gráfico de Violín (Violin Plot)
grafico_pdet_violin <- base_completa %>%
  filter(pdet %in% c(0,1)) %>%
  pivot_longer(cols = all_of(vars_interes_compl), names_to = "variable", values_to = "valor") %>%
  mutate(variable = factor(variable,
                           levels = vars_interes_compl,
                           labels = nombres_descriptivos_compl)) %>%
  ggplot(aes(x = factor(pdet), y = valor, fill = factor(pdet))) +
  geom_violin(trim = TRUE) + # Agrega la capa de violín
  # Puedes añadir un boxplot o puntos para ver la mediana/cuartiles
  geom_boxplot(width = 0.1, color = "black", alpha = 0.5) +
  facet_wrap(~variable, scales = "free") +
  scale_fill_manual(values = c("0" = "lightblue", "1" = "salmon"), labels = c("No PDET", "PDET")) +
  labs(
    x = "Grupo PDET", 
    y = "Valor", 
    fill = "Grupo", 
    title = "Comparación de Distribuciones por Grupo PDET (Gráfico de Violín)"
  ) +
  theme_minimal()

# Guardar el gráfico
ggsave("Graficos-tablas/grafico_compl_violin_pdet.png", grafico_pdet_violin, width = 10, height = 7, dpi = 300)

# Tabla de estadísticas descriptivas de homicidios por grupo PDET
tabla_visitantes <- base_completa %>%
  filter(!is.na(pdet), !is.na(visitantes_extranjeros_no_residentes)) %>%
  group_by(codmpio, pdet) %>%                     # agrupa por municipio y grupo PDET
  summarise(visitantes_extranjeros_no_residentes_total = sum(visitantes_extranjeros_no_residentes, na.rm = TRUE), .groups = "drop") %>%
  group_by(pdet) %>%
  summarise(
    N_municipios = n(),                            # ahora sí cuenta municipios únicos
    Min = min(visitantes_extranjeros_no_residentes_total),
    Q1 = quantile(visitantes_extranjeros_no_residentes_total, 0.25),
    Promedio = mean(visitantes_extranjeros_no_residentes_total),
    Mediana = median(visitantes_extranjeros_no_residentes_total),
    Q3 = quantile(visitantes_extranjeros_no_residentes_total, 0.75),
    Max = max(visitantes_extranjeros_no_residentes_total),
    Desviación = sd(visitantes_extranjeros_no_residentes_total)
  ) %>%
  mutate(Grupo = ifelse(pdet == 1, "PDET", "No PDET")) %>%
  select(Grupo, everything(), -pdet)

# Tabla HTML
tabla_html_visitantes <- tabla_visitantes %>%
  kable(
    caption = "Estadísticas descriptivas de visitantes extranjeros no residentes por grupo PDET (nivel municipal)",
    digits = 2,
    col.names = c("Grupo", "N", "Mínimo", "1er Cuartil", "Promedio", "Mediana", "3er Cuartil", "Máximo", "Desviación estándar")
  ) %>%
  kable_classic(full_width = FALSE, html_font = "Cambria")

# Guardar como HTML
save_kable(tabla_html_visitantes, "Graficos-tablas/tabla_visitantes.html")
```

## Paso 4. Modelo empírico

## Paso 4.1 Propensity Score Matching

```{r}
# Revisar la estructura inicial de la base
str(base_completa)

# Convertir variables turísticas a numéricas y asegurar que pdet sea 0 o 1
base_completa <- base_completa %>%
  mutate(
    across(
      c(
        llegadas_pasajeros_nacionales,
        llegadas_internacionales,
        visitantes_extranjeros_no_residentes,
        prestadores_de_servicios_turisticos_activos,
        establecimientos_de_alojamiento_y_hospedaje,
        visitantes_pnn
      ),
      ~ as.numeric(.)
    ),
    pdet = case_when(
      pdet %in% c(1, "1", "PDET", "Sí", "Si") ~ 1,
      pdet %in% c(0, "0", "No PDET", "No") ~ 0,
      TRUE ~ 0  
    )
  )

# Revisar estructura después de las conversiones
str(base_completa)

# Eliminar filas donde pdet está en NA 
base_completa <- base_completa %>%
  filter(!is.na(pdet))

# Eliminar filas con NA en variables clave
base_completa <- base_completa %>%
  filter(
    !if_any(
      c(
        discapital, altura, indrural, df_desemp_fisc, homicidios,
        poblacion, t_establ_o, act_primarias, act_secundarias, act_terciarias
      ),
      is.na
    )
  )


base_completa <- base_completa %>%
 mutate(
 pdet = as.numeric(pdet),
 region = as.factor(region),
    codmpio = as.factor(codmpio),
    ano = as.factor(ano) 
 )
# Verificar que pdet solo tenga 0 y 1
table(base_completa$pdet)
unique(base_completa$pdet)

# Ejecutar el PSM por región con transformaciones y método "optimal"
m_caliper <- matchit(
  formula = pdet ~ log1p(poblacion) + log1p(homicidios) + log1p(t_establ_o) +
    indrural + df_desemp_fisc + discapital + act_primarias +
    act_secundarias + act_terciarias,
  data = base_completa,
  method = "nearest",     # emparejamiento por vecino más cercano
  distance = "logit",     # estima el propensity score mediante regresión logística
  caliper = 0.15,          # límite máximo permitido de diferencia en el propensity score
  replace = FALSE          
)

# Verificar el balance gráficamente 
love.plot(m_caliper, threshold = 0.1, abs = TRUE)

# Extraer los datos emparejados
matched_data <- match.data(m_caliper)

# Verificar distribución después del matching
table(matched_data$pdet)

# Revisar si quedó bien el matching
resumen_psm <- summary(m_caliper, standardize = TRUE)

# Extraer balances antes y después del matching
antes <- as.data.frame(resumen_psm$sum.all)      # Balance antes del matching
despues <- as.data.frame(resumen_psm$sum.matched) # Balance después del matching

# Comparar reducción del sesgo estandarizado
comparacion_sesgo <- data.frame(
  Variable = rownames(antes),
  Sesgo_Antes = antes$`Std. Mean Diff.`,
  Sesgo_Despues = despues$`Std. Mean Diff.`
)

# Visualizar diferencias
print(comparacion_sesgo)

# Promedio del sesgo antes y después (para verificar mejora)
mean(abs(comparacion_sesgo$Sesgo_Antes), na.rm = TRUE)
mean(abs(comparacion_sesgo$Sesgo_Despues), na.rm = TRUE)

# guardar
tabla_balance <- stargazer(
  comparacion_sesgo,
  summary = FALSE,         
  rownames = FALSE,        
  type = "html",          
  title = "Balance de Covariables Antes y Después del Matching",
  covariate.labels = c("Variable", "Sesgo Antes", "Sesgo Después"),
  digits = 3,              
  out = "Graficos-tablas/balance_matching_AER.html", 
  no.space = TRUE,         
  header = FALSE,          
  align = TRUE,
  table.layout = "=ldc-t-", 
  font.size = "footnotesize"
)

# Para ver la tabla en la consola, puedes usar:
stargazer(comparacion_sesgo, summary = FALSE, rownames = FALSE, type = "html")

# Gráfico de PSM
grafico_sesgo <- ggplot(comparacion_sesgo, 
                        aes(x = reorder(Variable, Sesgo_Antes))) +
  geom_point(aes(y = Sesgo_Antes, color = "Antes"), size = 3, alpha = 0.8) +
  geom_point(aes(y = Sesgo_Despues, color = "Después"), size = 3, alpha = 0.8) +
  geom_hline(yintercept = c(-0.1, 0.1), linetype = "dashed", color = "gray40") +
  coord_flip() +
  labs(
    title = "Comparación del Sesgo Estandarizado Antes y Después del Matching",
    x = NULL,
    y = "Sesgo Estandarizado",
    color = NULL
  ) +
  scale_color_manual(values = c("Antes" = "#B22222", "Después" = "#1E90FF")) +
  theme_minimal(base_family = "serif", base_size = 13) +
  theme(
    plot.title = element_text(face = "bold", size = 14, hjust = 0.5),
    axis.text = element_text(color = "black"),
    legend.position = "top",
    legend.justification = "center",
    panel.grid.major.y = element_blank(),
    panel.grid.minor = element_blank(),
    panel.grid.major.x = element_line(color = "gray85")
  )

# Mostrar el gráfico
grafico_sesgo

# Guardar 
ggsave("Graficos-tablas/comparacion_sesgo_AER.png", grafico_sesgo, 
       width = 8, height = 6, dpi = 300)

png("Graficos-tablas/hist_matching_caliper.png", width = 1200, height = 800, res = 150)

plot(m_caliper, type = "hist")
dev.off()

# Para ver las observaciones que hicieron matching
datos_emparejados <- match.data(m_caliper)

# Visualiza las primeras filas y su estructura para confirmar
head(datos_emparejados)
str(datos_emparejados)

# El número de filas será la muestra reducida y emparejada
nrow(datos_emparejados)

```

## Paso 4.2 Tendencias paralelas

```{r}
# Crear el año del programa
datos_emparejados <- datos_emparejados %>%
  mutate(
    ano = as.numeric(as.character(ano)),  # asegurar que sea numérica
    event_time = ano - 2017
  )

table(datos_emparejados$event_time)

# Modelo 
modelo_evento <- feols(
  log1p(visitantes_extranjeros_no_residentes) ~ i(event_time, pdet, ref = -1) | codmpio + ano,
  cluster = ~region,
  data = datos_emparejados
)

# Guadar gráfico
png("Graficos-tablas/fig_event_study_PDET.png", width = 900, height = 600, res = 150)

iplot(
  modelo_evento,
  ref.line = 0,
  main = "Tendencias Paralelas PDET sobre el turismo",
  xlab = "Tiempo",
  ylab = "Visitantes extranjeros no residentes",
  col = "black",
  ci.col = "grey60",
  lwd = 2,
  ci.lwd = 1.2,
  grid = FALSE
)

abline(v = 0, lty = 2, col = "darkred")   # línea vertical en el evento
abline(h = 0, lty = 1, col = "gray40")    # línea horizontal en cero
dev.off()


```

## Paso 4.3. Modelos DiD

```{r}
# Se escoge el año de programa
datos_emparejados <- datos_emparejados %>%
  mutate(
    post = as.numeric(ano >= 2017) # 1 si el año es 2017 o posterior, 0 si es anterior
  )


# Modelo 2 con controles
modelo_did_psm_sinfe <- felm(
 log1p(visitantes_extranjeros_no_residentes) ~ pdet + post + pdet*post +
  log1p(poblacion) +
  log1p(homicidios) +
  log1p(t_establ_o) +
  df_desemp_fisc +
  act_terciarias 
  | 0  
  | 0
  | region,
 data = datos_emparejados
)

modelo_did_psm_mpiofe <- felm(
 log1p(visitantes_extranjeros_no_residentes) ~ pdet + post + pdet*post +
  log1p(poblacion) +
  log1p(homicidios) +
  log1p(t_establ_o) +
  df_desemp_fisc +
  act_terciarias 
  | codmpio + ano
  | 0
  | region,
 data = datos_emparejados
)



stargazer(
 modelo_did_psm_mpiofe,
 modelo_did_psm_sinfe,
 title = "Impacto del PDET (DiD con PSM): Comparación de Modelos",
 out = "Graficos-tablas/modelo_did_psm_comparativa.html",
 type = "text",
 
 # Ajuste de etiquetas 
 covariate.labels = c(
  "Tratamiento (PDET)",              
  "Post-Tratamiento (Post 2017)",    
  "PDET $\\times$ Post (ATT)",       
  "Población (Log)",
  "Homicidios (Log)",
  "Establecimientos (Log)",
  "Desempeño Fiscal",
  "Act. Terciarias"
 ),
 dep.var.labels = "Log(1+Visitantes Extranjeros)",
 
 # Añadir filas para las estadísticas y los Efectos Fijos
 add.lines = list(
  c("Efectos Fijos de Municipio", "Sí", "Sí"),
  c("Efectos Fijos de Año", "Sí", "Sí"),
  c("Errores Estándar Agrupados", "Región", "Región")
 ),
 
 # Etiquetas de columnas
 column.labels = c("con efectos mpio", "sin efectos")

)
```

## Paso 4.4 Pruebas de robustez

```{r}

# Fórmula del PSM 
psm_formula <- pdet ~ log1p(poblacion) + log1p(homicidios) + log1p(t_establ_o) +
  indrural + df_desemp_fisc + act_primarias + act_secundarias + act_terciarias

# Matching con Caliper 0.15 
m_caliper_base <- matchit(
  formula = psm_formula,
  data = base_completa,
  method = "nearest",
  distance = "logit",
  caliper = 0.15,
  replace = FALSE
)

# Matching con Caliper 0.10 
m_caliper_estricto <- matchit(
  formula = psm_formula,
  data = base_completa,
  method = "nearest",
  distance = "logit",
  caliper = 0.10,
  replace = FALSE
)

# Tablas de balance separadas 
balance_base <- bal.tab(m_caliper_base, stats = "mean.diffs", disp.smd = TRUE)
balance_estricto <- bal.tab(m_caliper_estricto, stats = "mean.diffs", disp.smd = TRUE)

#  Mostrar los resultados 
print(balance_base)
print(balance_estricto)

# Guardar resultados

# Extraer balance base
tab_base <- as.data.frame(balance_base$Balance) %>%
  tibble::rownames_to_column(var = "Variable") %>%
  dplyr::select(Variable, Type, Diff.Adj) %>%
  rename(Diff_Adj_0.15 = Diff.Adj)

# Extraer balance estricto
tab_estricto <- as.data.frame(balance_estricto$Balance) %>%
  tibble::rownames_to_column(var = "Variable") %>%
  dplyr::select(Variable, Diff.Adj) %>%
  rename(Diff_Adj_0.10 = Diff.Adj)

# Unir (join) 
tabla_comparada <- dplyr::left_join(tab_base, tab_estricto, by = "Variable") %>%
  mutate(Dif_abs = abs(Diff_Adj_0.10) - abs(Diff_Adj_0.15))

# Vector de nombres claros 
nombres_claros <- c(
  "distance" = "Puntaje de Propensión (Distance)",
  "log1p(poblacion)" = "Log(Población)",
  "log1p(homicidios)" = "Log(Homicidios)",
  "log1p(t_establ_o)" = "Log(Establecimientos Totales)",
  "indrural" = "Índice de Ruralidad",
  "df_desemp_fisc" = "Déficit Fiscal / Desempleo",
  "act_primarias" = "Actividad Primaria (%)",
  "act_secundarias" = "Actividad Secundaria (%)",
  "act_terciarias" = "Actividad Terciaria (%)"
)

# Limpiar y renombrar 
tabla_comparada_limpia <- tabla_comparada %>%
  mutate(Variable = dplyr::recode(Variable, !!!nombres_claros)) %>%
  select(Variable, Type, Diff_Adj_0.15, Diff_Adj_0.10, Dif_abs) %>%
  distinct()

# Generar tabla HTML 
tabla_comparada_limpia %>%
  kbl(
    caption = "Comparación de balance entre calipers 0.15 y 0.10",
    digits = 4, align = "c", booktabs = TRUE,
    col.names = c("Variable", "Tipo", "Caliper 0.15", "Caliper 0.10", "Diferencia Abs")
  ) %>%
  kable_paper("hover", full_width = FALSE) %>%
  add_header_above(c(" " = 2, "Diferencias Estandarizadas de Medias" = 2, " " = 1)) %>%
  save_kable("Graficos-tablas/balance_comparados.html")

print("Archivo 'balance_comparado_nombres.html' guardado con éxito.")


```

```{r}
# Prueba placebo
datos_emparejados <- datos_emparejados %>%
  mutate(
    # Variable post real (para el modelo base)
    post_real = as.numeric(ano >= 2017), 
    
    # Variable post placebo (asume que el tratamiento inició en 2015)
    post_placebo = as.numeric(ano >= 2015)
  )

#Se corre el modelo con el post placebo 
modelo_placebo <- felm(
  log1p(visitantes_extranjeros_no_residentes) ~ pdet * post_placebo + 
    log1p(poblacion) + log1p(homicidios) + log1p(t_establ_o) +
    df_desemp_fisc + act_primarias + act_secundarias + act_terciarias
    | codmpio + ano 
    | 0 
    | region, 
  data = datos_emparejados
)

# M1 (Base)
coef_real <- coef(modelo_did_psm_control)["pdet:post"] 
se_real <- se(modelo_did_psm_control)["pdet:post"]

# M2 (Placebo)
coef_placebo <- coef(modelo_placebo)["pdet:post_placebo"]
se_placebo <- se(modelo_placebo)["pdet:post_placebo"]


# --- Generación de Tabla con Stargazer (Con corrección de \\_ ) ---

stargazer(
   modelo_did_psm_control,
   modelo_placebo,
   title = "Prueba de Placebo: Falsificación del Inicio del Tratamiento",
   type = "text",
 
 # Omitir todos los coeficientes del cuerpo principal
   omit = ".*", 
   omit.stat = c("f", "ser", "rsq"),
 
 # Insertar el ATT real y el Placebo de forma manual sin estrellas
 add.lines = list(
 c("ATT (Efecto Real 2017)", 
 sprintf("%.4f", coef_real), 
 sprintf("%.4f", coef_placebo)
 ),
 c("Error Estándar", 
 sprintf("(%.4f)", se_real), 
 sprintf("(%.4f)", se_placebo)
 ),
 c("", "", ""),
 c("Efectos Fijos de Municipio", "Sí", "Sí"),
 c("Efectos Fijos de Año", "Sí", "Sí")
 ),

 column.labels = c("Modelo Base", "Modelo Placebo"),
 header = FALSE,
 no.space = TRUE
)
```

## Paso 5. Modelo empírico (efectos heterogeneos)

(zaira) \## Paso 5.1 Propensity Score Matching \## Paso 5.2 Tendencias paralelas \## Paso 5.3 Modelos DiD \## Paso 5.4 Pruebas de robustez

## Paso 5.1 Propensity Score Matching

```{r}
# Nueva base con municipios PDET con  vocacióon turística

# Seleccionar los municipios con vocación
codigos_turismo <- c(
  "20570", "20001", "44650", "20013", "20385", "20400", "20443", "20750", 
  "44090", "44279", "47053", "47189", "47288", "47001", "18592", "50370", 
  "95001", "50370", "50330", "50683", "50400", "18094", "18150", "18247", 
  "18256", "18001", "18410", "18479", "18592", "18610", "18753", "52835", 
  "52079", "13212", "13244", "73319", "13654", "13657", "70230", "70713", 
  "70204", "70473", "70508", "70418", "19698", "19455", "19743", "19585", 
  "19780", "76109", "19809", "81065", "81736", "81794", "86001", "86573", 
  "68686", "86865", "86885", "86569", "86320", "05045", "05147", "05172", 
  "05234", "05480", "05490", "05665", "05837", "27800", "27006", "17614", 
  "27361", "27495", "99001", "99773", "54250", "54245", "54720", "54261", 
  "54874", "23068", "23807", "23855", "73555", "73168", "15236", "15533", 
  "25878", "25438", "05861", "17042", "17088", "17616", "85125", "13873", 
  "11001"
)

# Creación de la nueva base de datos filtrada
base_voc_turismo <- base_completa %>%
  filter(
    pdet == 0 | 
    (pdet == 1 & codmpio %in% codigos_turismo)
  )

# Contar la frecuencia de cada valor en la columna 'pdet'
conteo_voc_turismo <- base_voc_turismo %>%
  count(pdet)

# Verificar la estructura de la nueva base
str(base_voc_turismo)

# Eliminar filas donde pdet está en NA 
base_voc_turismo <- base_voc_turismo %>%
  filter(!is.na(pdet))

# Eliminar filas con NA en variables clave
base_voc_turismo <- base_voc_turismo %>%
  filter(
    !if_any(
      c(
        discapital, altura, indrural, df_desemp_fisc, homicidios,
        poblacion, t_establ_o, act_primarias, act_secundarias, act_terciarias
      ),
      is.na
    )
  )

# Verificar que pdet solo tenga 0 y 1
unique(base_voc_turismo$pdet)

# Ejecutar el PSM por región con transformaciones y método "optimal"
m_caliper_turismo <- matchit(
  formula = pdet ~ log1p(poblacion) + log1p(homicidios) + log1p(t_establ_o) +
    indrural + df_desemp_fisc + discapital + act_primarias +
    act_secundarias + act_terciarias,
  data = base_voc_turismo,
  method = "nearest",     # emparejamiento por vecino más cercano
  distance = "logit",     # estima el propensity score mediante regresión logística
  caliper = 0.15,          # límite máximo permitido de diferencia en el propensity score
  replace = FALSE          
)

# Verificar el balance gráficamente 
love.plot(m_caliper_turismo, threshold = 0.1, abs = TRUE)

# Extraer los datos emparejados
matched_data_turismo <- match.data(m_caliper_turismo)

# Verificar distribución después del matching
table(matched_data_turismo$pdet)

# Revisar si quedó bien el matching
resumen_psm_turismo <- summary(m_caliper_turismo, standardize = TRUE)

# Extraer balances antes y después del matching
antes_turismo <- as.data.frame(resumen_psm_turismo$sum.all)      # Balance antes del matching
despues_turismo <- as.data.frame(resumen_psm_turismo$sum.matched) # Balance después del matching

# Comparar reducción del sesgo estandarizado
comparacion_sesgo_turismo <- data.frame(
  Variable = rownames(antes_turismo),
  Sesgo_Antes_t = antes_turismo$`Std. Mean Diff.`,
  Sesgo_Despues_t = despues_turismo$`Std. Mean Diff.`
)

# Visualizar diferencias
print(comparacion_sesgo_turismo)

# Promedio del sesgo antes y después (para verificar mejora)
mean(abs(comparacion_sesgo_turismo$Sesgo_Antes_t), na.rm = TRUE)

mean(abs(comparacion_sesgo_turismo$Sesgo_Despues_t), na.rm = TRUE)

# guardar
tabla_balance_turismo <- stargazer(
  comparacion_sesgo_turismo,
  summary = FALSE,         
  rownames = FALSE,        
  type = "html",          
  title = "Balance de Covariables Antes y Después del Matching vocación turistica",
  covariate.labels = c("Variable", "Sesgo Antes", "Sesgo Después"),
  digits = 3,              
  out = "Graficos-tablas/balance_matching_AER_turismo.html", 
  no.space = TRUE,         
  header = FALSE,          
  align = TRUE,
  table.layout = "=ldc-t-", 
  font.size = "footnotesize"
)

# Para ver la tabla en la consola, puedes usar:
stargazer(comparacion_sesgo_turismo, summary = FALSE, rownames = FALSE, type = "html")

# Gráfico de PSM
grafico_sesgo_turismo <- ggplot(comparacion_sesgo_turismo, 
                        aes(x = reorder(Variable, Sesgo_Antes_t))) +
  geom_point(aes(y = Sesgo_Antes_t, color = "Antes"), size = 3, alpha = 0.8) +
  geom_point(aes(y = Sesgo_Despues_t, color = "Después"), size = 3, alpha = 0.8) +
  geom_hline(yintercept = c(-0.1, 0.1), linetype = "dashed", color = "gray40") +
  coord_flip() +
  labs(
    title = "Comparación del Sesgo Estandarizado Turismo Antes y Después del Matching",
    x = NULL,
    y = "Sesgo Estandarizado",
    color = NULL
  ) +
  scale_color_manual(values = c("Antes" = "#B22222", "Después" = "#1E90FF")) +
  theme_minimal(base_family = "serif", base_size = 13) +
  theme(
    plot.title = element_text(face = "bold", size = 14, hjust = 0.5),
    axis.text = element_text(color = "black"),
    legend.position = "top",
    legend.justification = "center",
    panel.grid.major.y = element_blank(),
    panel.grid.minor = element_blank(),
    panel.grid.major.x = element_line(color = "gray85")
  )

# Mostrar el gráfico
grafico_sesgo_turismo

# Guardar 
ggsave("Graficos-tablas/comparacion_sesgo_turismo_AER.png", grafico_sesgo_turismo, 
       width = 8, height = 6, dpi = 300)

#png("Graficos-tablas/hist_matching_caliper.png", width = 1200, height = 800, res = 150)

plot(m_caliper_turismo, type = "hist")
dev.off()

# Para ver las observaciones que hicieron matching
datos_emparejados_turismo <- match.data(m_caliper_turismo)

# Visualiza las primeras filas y su estructura para confirmar
head(datos_emparejados_turismo)
str(datos_emparejados_turismo)

# El número de filas será la muestra reducida y emparejada
nrow(datos_emparejados_turismo)
```

## Paso 5.2 Tendencias paralelas

```{r}
# Crear el año del programa
datos_emparejados_turismo <- datos_emparejados_turismo %>%
  mutate(
    ano = as.numeric(as.character(ano)),  # asegurar que sea numérica
    event_time = ano - 2017
  )

table(datos_emparejados_turismo$event_time)

# Modelo 
modelo_evento_turismo <- feols(
  log1p(visitantes_extranjeros_no_residentes) ~ i(event_time, pdet, ref = -1) | codmpio + ano,
  cluster = ~region,
  data = datos_emparejados_turismo
)

# Guadar gráfico
png("Graficos-tablas/fig_event_study_turismo_PDET.png", width = 900, height = 600, res = 150)

iplot(
  modelo_evento_turismo,
  ref.line = 0,
  main = "Tendencias Paralelas PDET con vocación turista sobre el turismo",
  xlab = "Tiempo",
  ylab = "Visitantes extranjeros no residentes",
  col = "black",
  ci.col = "grey60",
  lwd = 2,
  ci.lwd = 1.2,
  grid = FALSE
)

abline(v = 0, lty = 2, col = "darkred")   # línea vertical en el evento
abline(h = 0, lty = 1, col = "gray40")    # línea horizontal en cero
dev.off()
```

## Paso 5.3 Modelo DiD

```{r}
# Se escoge el año de programa
datos_emparejados_turismo <- datos_emparejados_turismo %>%
  mutate(
    post = as.numeric(ano >= 2017) # 1 si el año es 2017 o posterior, 0 si es anterior
  )
# Modelo 2 con controles
modelo_did_psm_sinfe_turismo <- felm(
 log1p(visitantes_extranjeros_no_residentes) ~ pdet + post + pdet*post +
  log1p(poblacion) +
  log1p(homicidios) +
  log1p(t_establ_o) +
  df_desemp_fisc +
  act_terciarias 
  | 0  
  | 0
  | region,
 data = datos_emparejados_turismo
)

modelo_did_psm_mpiofe_turismo <- felm(
 log1p(visitantes_extranjeros_no_residentes) ~ pdet + post + pdet*post +
  log1p(poblacion) +
  log1p(homicidios) +
  log1p(t_establ_o) +
  df_desemp_fisc +
  act_terciarias 
  | codmpio + ano
  | 0
  | region,
 data = datos_emparejados_turismo
)



stargazer(
 modelo_did_psm_mpiofe_turismo,
 modelo_did_psm_sinfe_turismo,
 title = "Impacto del PDET (DiD con PSM): Comparación de Modelos municipios con vocación turística",
 out = "Graficos-tablas/modelo_did_psm_comparativa_voc_turistica.html",
 type = "text",
 
 # Ajuste de etiquetas 
 covariate.labels = c(
  "Tratamiento (PDET)",              
  "Post-Tratamiento (Post 2017)",    
  "PDET $\\times$ Post (ATT)",       
  "Población (Log)",
  "Homicidios (Log)",
  "Establecimientos (Log)",
  "Desempeño Fiscal",
  "Act. Terciarias"
 ),
 dep.var.labels = "Log(1+Visitantes Extranjeros)",
 
 # Añadir filas para las estadísticas y los Efectos Fijos
 add.lines = list(
  c("Efectos Fijos de Municipio", "Sí", "Sí"),
  c("Efectos Fijos de Año", "Sí", "Sí"),
  c("Errores Estándar Agrupados", "Región", "Región")
 ),
 
 # Etiquetas de columnas
 column.labels = c("con efectos mpio", "sin efectos")

)
```

```{r}
# modelos sin clusterizar los errores por región sino por departamentos

base_voc_turismo <- base_voc_turismo %>%
  mutate(
    departamento = str_sub(codmpio, 1, 2)
  )

# Nuevos modelos
# Se escoge el año de programa
datos_emparejados_turismo <- datos_emparejados_turismo %>%
  mutate(
    post = as.numeric(ano >= 2017) # 1 si el año es 2017 o posterior, 0 si es anterior
  )
# Modelo 2 con controles
modelo_did_psm_sinfe_turismo2 <- felm(
 log1p(visitantes_extranjeros_no_residentes) ~ pdet + post + pdet*post +
  log1p(poblacion) +
  log1p(homicidios) +
  log1p(t_establ_o) +
  df_desemp_fisc +
  act_terciarias 
  | 0  
  | 0
  | departamento,
 data = datos_emparejados_turismo
)

modelo_did_psm_mpiofe_turismo2 <- felm(
 log1p(visitantes_extranjeros_no_residentes) ~ pdet + post + pdet*post +
  log1p(poblacion) +
  log1p(homicidios) +
  log1p(t_establ_o) +
  df_desemp_fisc +
  act_terciarias 
  | codmpio + ano
  | 0
  | departamento,
 data = datos_emparejados_turismo
)



stargazer(
 modelo_did_psm_mpiofe_turismo2,
 modelo_did_psm_sinfe_turismo2,
 title = "Impacto del PDET (DiD con PSM): Comparación de Modelos 2 municipios con vocación turística",
 out = "Graficos-tablas/modelo_did_psm_comparativa_voc_turistica2.html",
 type = "text",
 
 # Ajuste de etiquetas 
 covariate.labels = c(
  "Tratamiento (PDET)",              
  "Post-Tratamiento (Post 2017)",    
  "PDET $\\times$ Post (ATT)",       
  "Población (Log)",
  "Homicidios (Log)",
  "Establecimientos (Log)",
  "Desempeño Fiscal",
  "Act. Terciarias"
 ),
 dep.var.labels = "Log(1+Visitantes Extranjeros)",
 
 # Añadir filas para las estadísticas y los Efectos Fijos
 add.lines = list(
  c("Efectos Fijos de Municipio", "Sí", "Sí"),
  c("Efectos Fijos de Año", "Sí", "Sí"),
  c("Errores Estándar Agrupados", "Región", "Región")
 ),
 
 # Etiquetas de columnas
 column.labels = c("con efectos mpio", "sin efectos")

)
```

## Paso 5.4 Pruebas de robustez

```{r}
# Fórmula del PSM 
psm_formula_turismo <- pdet ~ log1p(poblacion) + log1p(homicidios) + log1p(t_establ_o) +
  indrural + df_desemp_fisc + act_primarias + act_secundarias + act_terciarias

# Matching con Caliper 0.15 
m_caliper_base_turismo <- matchit(
  formula = psm_formula_turismo,
  data = base_voc_turismo,
  method = "nearest",
  distance = "logit",
  caliper = 0.15,
  replace = FALSE
)

# Matching con Caliper 0.10 
m_caliper_estricto_turismo <- matchit(
  formula = psm_formula_turismo,
  data = base_voc_turismo,
  method = "nearest",
  distance = "logit",
  caliper = 0.10,
  replace = FALSE
)

# Tablas de balance separadas 
balance_base_turismo <- bal.tab(m_caliper_base_turismo, stats = "mean.diffs", disp.smd = TRUE)
balance_estricto_turismo <- bal.tab(m_caliper_estricto_turismo, stats = "mean.diffs", disp.smd = TRUE)

#  Mostrar los resultados 
print(balance_base_turismo)
print(balance_estricto_turismo)

# Guardar resultados

# Extraer balance base
tab_base_turismo <- as.data.frame(balance_base_turismo$Balance) %>%
  tibble::rownames_to_column(var = "Variable") %>%
  dplyr::select(Variable, Type, Diff.Adj) %>%
  rename(Diff_Adj_0.15 = Diff.Adj)

# Extraer balance estricto
tab_estricto_turismo <- as.data.frame(balance_estricto_turismo$Balance) %>%
  tibble::rownames_to_column(var = "Variable") %>%
  dplyr::select(Variable, Diff.Adj) %>%
  rename(Diff_Adj_0.10 = Diff.Adj)

# Unir (join) 
tabla_comparada_turismo <- dplyr::left_join(tab_base_turismo, tab_estricto_turismo, by = "Variable") %>%
  mutate(Dif_abs = abs(Diff_Adj_0.10) - abs(Diff_Adj_0.15))

# Vector de nombres claros 
nombres_claros_turismo <- c(
  "distance" = "Puntaje de Propensión (Distance)",
  "log1p(poblacion)" = "Log(Población)",
  "log1p(homicidios)" = "Log(Homicidios)",
  "log1p(t_establ_o)" = "Log(Establecimientos Totales)",
  "indrural" = "Índice de Ruralidad",
  "df_desemp_fisc" = "Déficit Fiscal / Desempleo",
  "act_primarias" = "Actividad Primaria (%)",
  "act_secundarias" = "Actividad Secundaria (%)",
  "act_terciarias" = "Actividad Terciaria (%)"
)

# Limpiar y renombrar 
tabla_comparada_limpia_turismo <- tabla_comparada_turismo %>%
  mutate(Variable = dplyr::recode(Variable, !!!nombres_claros_turismo)) %>%
  select(Variable, Type, Diff_Adj_0.15, Diff_Adj_0.10, Dif_abs) %>%
  distinct()

# Generar tabla HTML 
tabla_comparada_limpia_turismo %>%
  kbl(
    caption = "Comparación de balance entre calipers 0.15 y 0.10",
    digits = 4, align = "c", booktabs = TRUE,
    col.names = c("Variable", "Tipo", "Caliper 0.15", "Caliper 0.10", "Diferencia Abs")
  ) %>%
  kable_paper("hover", full_width = FALSE) %>%
  add_header_above(c(" " = 2, "Diferencias Estandarizadas de Medias" = 2, " " = 1)) %>%
  save_kable("Graficos-tablas/balance_comparados_vocación turistica.html")

print("Archivo 'balance_comparado_voc_turismo_nombres.html' guardado con éxito.")

```

```{r}
# Prueba placebo
datos_emparejados_turismo <- datos_emparejados_turismo %>%
  mutate(
    # Variable post real (para el modelo base)
    post_real_turismo = as.numeric(ano >= 2017), 
    
    # Variable post placebo (asume que el tratamiento inició en 2015)
    post_placebo_turismo = as.numeric(ano >= 2015)
  )

#Se corre el modelo con el post placebo 
modelo_placebo_turismo <- felm(
  log1p(visitantes_extranjeros_no_residentes) ~ pdet * post_placebo_turismo + 
    log1p(poblacion) + log1p(homicidios) + log1p(t_establ_o) +
    df_desemp_fisc + act_primarias + act_secundarias + act_terciarias
    | codmpio + ano 
    | 0 
    | region, 
  data = datos_emparejados_turismo
)

# M1 (Base)
coef_real_turismo <- coef(modelo_did_psm_control_turismo)["pdet:post"] 
se_real_turismo <- se(modelo_did_psm_control_turismo)["pdet:post"]

# M2 (Placebo)
coef_placebo_turismo <- coef(modelo_placebo_turismo)["pdet:post_placebo"]
se_placebo_turismo <- se(modelo_placebo_turismo)["pdet:post_placebo"]


# --- Generación de Tabla con Stargazer (Con corrección de \\_ ) ---

stargazer(
   modelo_did_psm_control_turismo,
   modelo_placebo_turismo,
   title = "Prueba de Placebo vocación turistica: Falsificación del Inicio del Tratamiento",
   type = "text",
 
 # Omitir todos los coeficientes del cuerpo principal
   omit = ".*", 
   omit.stat = c("f", "ser", "rsq"),
 
 # Insertar el ATT real y el Placebo de forma manual sin estrellas
 add.lines = list(
 c("ATT (Efecto Real 2017)", 
 sprintf("%.4f", coef_real_turismo), 
 sprintf("%.4f", coef_placebo_turismo)
 ),
 c("Error Estándar", 
 sprintf("(%.4f)", se_real_turismo), 
 sprintf("(%.4f)", se_placebo_turismo)
 ),
 c("", "", ""),
 c("Efectos Fijos de Municipio", "Sí", "Sí"),
 c("Efectos Fijos de Año", "Sí", "Sí")
 ),

 column.labels = c("Modelo Base", "Modelo Placebo"),
 header = FALSE,
 no.space = TRUE
)
```
